# Tests

Lightweight tests will verify data conversion, configuration loading, metrics,
and other logic that does not require downloading a large model.

Run the current standard-library tests from the repository root:

```bash
python -m unittest discover -s tests -v
```
